'use strict';

/**
 * frontend-candidate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::frontend-candidate.frontend-candidate');
